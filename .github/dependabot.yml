version: 2

updates:
  # Rust (Cargo)
  - package-ecosystem: "cargo"
    directory: "/rust"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 10

  # GitHub Actions (workflow action versions)
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"


# plotly + hugo updates still missing
